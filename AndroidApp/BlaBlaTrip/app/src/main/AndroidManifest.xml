<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.tta.blablatrip">

    <!-- PERMISOS -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/blablatrip"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- TRADUCTOR -->
        <activity
            android:name=".view.TraductorActivity"
            android:label="@string/title_activity_traductor" />
        <!-- VALORAION -->
        <activity
            android:name=".view.ValoracionActivity"
            android:label="@string/title_activity_valoracion" />
        <!-- PAIS -->
        <activity
            android:name=".view.PaisActivity"
            android:label="@string/title_activity_pais" />
        <!-- EXPRESIONES -->
        <activity
            android:name=".view.ExpresionesActivity"
            android:label="@string/title_activity_expresiones" />
        <!-- TEST -->
        <activity
            android:name=".view.TestActivity"
            android:label="@string/title_activity_test" />

        <activity
            android:name=".view.TablaActivity"
            android:label="@string/table"/>
    </application>

</manifest>
